apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fraud-dataset-pvc
  namespace: fraud-mlops
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi